<div><h1>My projects</h1></div>
<br>

<div>
<ul class='unstyled'>
  <% @projects.each do |project| %>
    <li class='lead'>
      <%= link_to project.name, project_path(project) %>
    </li>
  <% end %>
</ul>
</div>